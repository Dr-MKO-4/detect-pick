# BEAC Anomaly Detector  Python dependencies
# Install: pip install -r requirements.txt
# Python 3.10+

# ── Core data science ────────────────────────────────────────────────────────
numpy>=1.24,<2.0
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3

# ── PyTorch (CPU build; replace with cu118/cu121 for GPU) ────────────────────
torch>=2.1
# For CUDA 11.8:  pip install torch>=2.1 --index-url https://download.pytorch.org/whl/cu118
# For CUDA 12.1:  pip install torch>=2.1 --index-url https://download.pytorch.org/whl/cu121

# ── Statistical decomposition ────────────────────────────────────────────────
statsmodels>=0.14

# ── Visualisation ────────────────────────────────────────────────────────────
plotly>=5.18
joblib>=1.3
dash>=2.14
dash-core-components>=2.0
dash-html-components>=2.0

# ── Web server (production mode) ─────────────────────────────────────────────
waitress>=3.0

# ── Explainability ───────────────────────────────────────────────────────────
shap>=0.44

# ── Desktop UI (optional  dev mode runs without PyQt6) ──────────────────────
PyQt6>=6.6
PyQt6-WebEngine>=6.6

# ── Export utilities ─────────────────────────────────────────────────────────
kaleido>=0.2              # PNG export for Plotly figures
openpyxl>=3.1             # Excel export
reportlab>=4.0            # PDF export (pur Python, pas de dépendance système)

# ── UMAP (optional  skip if not needed) ─────────────────────────────────────
# umap-learn>=0.5

# ── Packaging ────────────────────────────────────────────────────────────────
# pyinstaller>=6.0         # used only for .exe build, not needed at runtime
